{
  "name": "<%= props.name %>",
  "version": "1.0.0",
  "description": "<%= props.description %>",
  "main": "index.js",
  "scripts": {
    "start": "webpack --display-error-details",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "<%= props.author %> <<%= props.email %>>",
  "license": "<%= props.license %>",
  "devDependencies": {
    "mocha": "^2.5.3"
    ,"chai": "^3.5.0"
    ,"webpack": "^1.13.1"
    <% if( props.useLess ){ %>,"less-loader": "^2.2.3" <% } %>
  },
  "dependencies": {
  }
}
